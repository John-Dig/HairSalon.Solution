@{
  Layout = "_Layout";
}

@model HairSalon.Models.St

<h4>Add a new st</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <input type="submit" value="Add new st" />
}
<p>@Html.ActionLink("Show all sts", "Index")</p>